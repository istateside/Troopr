<div class="blog-intro-div">
  <figure class="blog-image-fig"><img src="<%= blog.get('filepicker_url') %>"></figure>
  <div class="blog-info">
    <h1><%= blog.escape('blogname') %></h1>
    <p><%= blog.escape('location') %></p>
    <p><%= blog.escape('description') %><p>

    <% if (!blog.get('is_current')) { %>
      <button class="follow-btn">
      <% if(blog.get('is_followed')) { %>Unfollow
      <% } else { %>Follow<% } %>
      </button>
    <% } %>

  </div>
</div>

<section class="posts-space blog-show"></section>
