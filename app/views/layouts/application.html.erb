<!DOCTYPE html>
<html>
	<head>
	  <title>Troopr</title>
	  <%= stylesheet_link_tag 'application', media: 'all' %>
	  <%= javascript_include_tag 'application' %>
	  <%= csrf_meta_tags %>
	</head>
	<body>
<script> window.current_user_id = <%= !!current_user ? current_user.id : 'false' %> </script>
			<%= render 'shared/navbar' %>
		<main>

			<%= render 'shared/errors' %>
			<%= yield %>
		</main>

	</body>
</html>
